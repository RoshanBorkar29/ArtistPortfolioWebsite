import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // This ensures paths map correctly on GitHub Pages where the site is served under /ArtistPortfolioWebsite/
  base: '/ArtistPortfolioWebsite/'
});
